#ifndef PLATFORM_PLATFORM_H_
#define PLATFORM_PLATFORM_H_

#include "platform/base/platform_type.h"

void platform_init(Platform_t const * const platform);
void platform_run(Platform_t const * const platform);

/* PLATFORM_PLATFORM_H_ */
#endif
